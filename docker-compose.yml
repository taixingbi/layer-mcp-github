# MCP streamable-http. Pass secrets via env_file (never bake .env into the image).
services:
  layer-mcp-github:
    image: layer-mcp-github
    build: .
    container_name: layer-mcp-github
    restart: unless-stopped
    labels:
      com.layer.service: layer-mcp-github
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      HTTP_HOST: "0.0.0.0"
      HTTP_PORT: "8000"
    extra_hosts:
      - "host.docker.internal:host-gateway"
